Act as the reserving analyst picking up an interrupted year-end valuation. The rebuild of the cumulative development triangle at `/app/data/development_triangle.json` was abandoned after its first development column, and the control-plane engine at `/app/workflow/develop_reserves.py` still evaluates winter draft conventions instead of the reserving committee's final decisions. Nothing the engine projects can be trusted until that triangle is rebuilt. The incremental claim movements and the period calendar that define it sit beside it under `/app/data`. Which movements reach the paid figure, how a recovery nets, which cell a movement lands in and what an empty lag holds are committee decisions, and the triangle must be restored at its expected path first.

Then restore the engine itself. Preserve its `--input` and `--output-dir` options and their defaults, and always read the period calendar and the reserving policy from their fixed absolute paths under `/app/data`; `--input` selects the triangle only. `/app/docs/report_spec.json` is the output contract: paths, schemas, required-field lists, coercions, container shapes and sort orders. It says nothing about how any value is derived. Reconstruct that from `/app/incident/reserving_governance_log.md`, mostly routine valuation notes, where the later dated decision governs. Each development row must also report how many distinct claims contribute to its cell and the largest claim's share of it; on a book this size those are bucketed once, not rescanned per cell, and a run must finish inside the contract's 120-second budget. A run writes exactly `/app/output/summary.json`, `/app/output/line_development.json` and `/app/output/reserve_queue.jsonl`. Derive every value from the operational inputs: standard library only, correct against an alternate triangle, identical across reruns, and leave the frozen snapshot in `/app/workflow` untouched.
