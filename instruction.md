The cumulative development triangle at `/app/data/development_triangle.json` ships empty, one bare object per line of business, and the engine at `/app/workflow/develop_reserves.py` still evaluates winter draft conventions rather than the reserving committee's final decisions, so nothing it projects is usable. Rebuild the triangle at that path first. The incremental claim movements at `/app/data/claim_movements.json` and the period calendar at `/app/data/period_calendar.json` are what define it, and the reserving policy the engine resolves against sits beside them at `/app/data/reserving_policy.json`. Which movements reach the paid figure, how a recovery nets, which cell a movement lands in and what an empty lag holds are committee decisions rather than yours, and they are recorded, along with everything the engine derives in the second step, in the minute book at `/app/incident/reserving_governance_log.md`.

Then restore the engine. Preserve its `--input` and `--output-dir` options and their defaults, and always read the period calendar and the reserving policy from their fixed absolute paths under `/app/data`; `--input` selects the triangle only. `/app/docs/report_spec.json` is the output contract: paths, schemas, required-field lists, coercions, container shapes and sort orders. It says nothing about how any value is derived. Reconstruct that from `/app/incident/reserving_governance_log.md`, most of which is routine valuation notes; where entries conflict, the later dated decision governs. Each development row also reports how many distinct claims contribute to its cell and the largest claim's share of it, and a run must finish inside the contract's 120-second budget, 120 seconds, on a book of this size. A run writes exactly `/app/output/summary.json`, `/app/output/line_development.json` and `/app/output/reserve_queue.jsonl`. Derive every value from the operational inputs, which are read rather than rewritten: the movement extract, the period calendar, the reserving policy and `/app/docs/report_spec.json` come back byte-identical. The triangle is the one thing under `/app/data` that gets replaced, and that replacement is the first step's, done once: the engine reads whatever triangle it is handed through `--input` and neither rebuilds nor rewrites one, so an engine that recomputes the triangle from the movements on each run is ignoring the input it was given. Standard library only, correct against an alternate triangle, identical across reruns, and leave the frozen snapshot `/app/workflow/.develop_reserves.original` exactly as it is.
